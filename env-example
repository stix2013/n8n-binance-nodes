N8N_VERSION="2.9.2"
# N8N
N8N_HOST=localhost
N8N_PORT=5678
N8N_HOST_PORT=5678
N8N_PROTOCOL=http
N8N_BASIC_AUTH_ACTIVE=true
N8N_BLOCK_ENV_ACCESS_IN_NODE=false
N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true
N8N_BASIC_AUTH_USER=your_admin_user
N8N_BASIC_AUTH_PASSWORD=your_secure_password
N8N_GIT_NODE_DISABLE_BARE_REPOS=false
N8N_DEV_RELOAD=true
N8N_COMMUNITY_PACKAGES_ENABLED=true
N8N_API_KEY="your_n8n_api_key"
N8N_API_URL="https://your-zrok-url.share.zrok.io"

# N8N Deprecation Fixes
N8N_MIGRATE_FS_STORAGE_PATH=true
OFFLOAD_MANUAL_EXECUTIONS_TO_WORKERS=true

# N8N Binance Node Config
N8N_BINANCE_API_URL="http://api:8000"
N8N_BINANCE_API_SPOT_PATH="http://api:8000"
N8N_BINANCE_API_FUTURE_PATH="http://api:8000"

# N8N Runners
N8N_RUNNERS_MODE=external
N8N_RUNNERS_BROKER_LISTEN_ADDRESS="0.0.0.0"
N8N_RUNNERS_AUTH_TOKEN="your_runners_auth_token"
N8N_NATIVE_PYTHON_RUNNER=true
N8N_RUNNERS_LAUNCHER_LOG_LEVEL=debug
N8N_RUNNERS_EXTERNAL_ALLOW=numpy,pandas,feedparser,requests,bs4,textblob,vaderSentiment,torch,datetime,urllib,quote
NODE_FUNCTION_ALLOW_EXTERNAL="moment,uuid,@binance/spot,@binance/common,trading-signals"

# Timezone
TZ="Asia/Jakarta"

# Database
DB_SQLITE_POOL_SIZE=2

# API
API_VERSION=1.6.0
API_PYTHON_VERSION=3.14
API_HOST=0.0.0.0
API_PORT=8000
API_HOST_PORT=8000
API_PROTOCOL=http
API_LOG_LEVEL=INFO
ENVIRONMENT=development

# Postgres
POSTGRES_DB=n8n
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_postgres_password

# Zrok (for webhooks)
WEBHOOK_URL="https://your-zrok-url.share.zrok.io"

# Zrok Configuration
ZROK_RESERVED_NAME="your-reserved-name"
ZROK_PUBLIC_URL="https://your-zrok-url.share.zrok.io"

# RabbitMQ
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=your_rabbitmq_password

# Binance API Credentials
BINANCE_API_KEY="your_binance_api_key"
BINANCE_SECRET_KEY="your_binance_api_secret"

# Trading Configuration (for futures proxy and candlestick sync)
# Comma-separated list of symbols to sync
TRADING_SYMBOLS="BTCUSDT,ETHUSDT,SOLUSDT,BNBUSDT"

# Comma-separated list of intervals to sync (1m, 5m, 15m, 30m, 1h, 2h, 4h, 6h, 8h, 12h, 1d, 3d, 1w, 1M)
TRADING_INTERVALS="1m,15m,1h,4h,1d"

# Sync interval in seconds (default: 60)
CANDLESTICK_SYNC_INTERVAL=60

# Market types to sync (spot, usd_m, coin_m)
TRADING_MARKET_TYPES="spot,usd_m"

# Binance Futures API URLs (optional - uses defaults if not set)
# BINANCE_FUTURES_USDM_BASE_URL="https://fapi.binance.com"
# BINANCE_FUTURES_COINM_BASE_URL="https://dapi.binance.com"
